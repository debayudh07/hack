# Environment variables for Base-Batch Finance contract
# Remove the .example extension and add your own values

# Your private key (without 0x prefix)
PRIVATE_KEY=your_private_key_without_0x_prefix

# Your Infura API key
INFURA_API_KEY=your_infura_api_key

# For gas estimations (optional, script uses default values if not provided)
ETH_PRICE_USD=3500
BASE_SEP_TOKEN_PRICE_USD=0.01
